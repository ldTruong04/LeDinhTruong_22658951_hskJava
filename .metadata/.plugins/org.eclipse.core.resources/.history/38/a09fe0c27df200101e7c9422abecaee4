package Tuan1;

import javax.swing.*;
import javax.swing.border.TitledBorder;
import java.awt.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class CongTruNhanChia extends JFrame implements ActionListener {

    private JTextField txtA, txtB, txtKetQua;
    private JButton btnGiai, btnXoa, btnThoat;
    private JRadioButton radCong, radTru, radNhan, radChia;
    private ButtonGroup group;

    public CongTruNhanChia() {
        setTitle("Cộng - Trừ - Nhân - Chia");
        setSize(520, 400);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
        setLayout(new BorderLayout());

        // NORTH
        JPanel pnNorth = new JPanel();
        JLabel lblTitle = new JLabel("Cộng Trừ Nhân Chia");
        lblTitle.setFont(new Font("Times New Roman", Font.BOLD, 22));
        lblTitle.setForeground(Color.BLUE);
        pnNorth.add(lblTitle);
        add(pnNorth, BorderLayout.NORTH);

        // WEST
        JPanel pnWest = new JPanel();
        pnWest.setLayout(new GridLayout(3, 1, 10, 10));
        pnWest.setBorder(BorderFactory.createTitledBorder(
                BorderFactory.createEtchedBorder(),
                "Chọn tác vụ",
                TitledBorder.CENTER,
                TitledBorder.TOP));

        btnGiai = new JButton("Giải");
        btnXoa = new JButton("Xóa");
        btnThoat = new JButton("Thoát");

        pnWest.add(btnGiai);
        pnWest.add(btnXoa);
        pnWest.add(btnThoat);

        add(pnWest, BorderLayout.WEST);

        //CENTER
        JPanel pnCenter = new JPanel();
        pnCenter.setLayout(null);
        pnCenter.setBorder(BorderFactory.createTitledBorder("Tính toán"));

        JLabel lblA = new JLabel("Nhập a:");
        lblA.setBounds(40, 40, 80, 25);
        pnCenter.add(lblA);

        txtA = new JTextField();
        txtA.setBounds(120, 40, 250, 25);
        pnCenter.add(txtA);

        JLabel lblB = new JLabel("Nhập b:");
        lblB.setBounds(40, 80, 80, 25);
        pnCenter.add(lblB);

        txtB = new JTextField();
        txtB.setBounds(120, 80, 250, 25);
        pnCenter.add(txtB);

        //Phép toán
        JPanel pnPhepToan = new JPanel();
        pnPhepToan.setLayout(new GridLayout(2, 2));
        pnPhepToan.setBorder(BorderFactory.createTitledBorder("Phép toán"));
        pnPhepToan.setBounds(120, 120, 250, 90);

        radCong = new JRadioButton("Cộng", true);
        radTru = new JRadioButton("Trừ");
        radNhan = new JRadioButton("Nhân");
        radChia = new JRadioButton("Chia");

        group = new ButtonGroup();
        group.add(radCong);
        group.add(radTru);
        group.add(radNhan);
        group.add(radChia);

        pnPhepToan.add(radCong);
        pnPhepToan.add(radTru);
        pnPhepToan.add(radNhan);
        pnPhepToan.add(radChia);

        pnCenter.add(pnPhepToan);

        JLabel lblKQ = new JLabel("Kết quả:");
        lblKQ.setBounds(40, 230, 80, 25);
        pnCenter.add(lblKQ);

        txtKetQua = new JTextField();
        txtKetQua.setBounds(120, 230, 250, 25);
        txtKetQua.setEditable(false);
        pnCenter.add(txtKetQua);

        add(pnCenter, BorderLayout.CENTER);

        //SOUTH 
        JPanel pnSouth = new JPanel();
        pnSouth.setBackground(Color.PINK);
        pnSouth.setPreferredSize(new Dimension(0, 50));

        pnSouth.add(new JButton());
        pnSouth.add(new JButton());
        pnSouth.add(new JButton());

        add(pnSouth, BorderLayout.SOUTH);

      
        btnGiai.addActionListener(this);
        btnXoa.addActionListener(this);
        btnThoat.addActionListener(this);
    }

    public static void main(String[] args) {
        SwingUtilities.invokeLater(() -> new CongTruNhanChia().setVisible(true));
    }

    @Override
    public void actionPerformed(ActionEvent e) {
        Object o = e.getSource();

        if (o.equals(btnGiai)) {
            xuLyGiai();
        } else if (o.equals(btnXoa)) {
            xuLyXoa();
        } else if (o.equals(btnThoat)) {
            xuLyThoat();
        }
    }

 

    private void xuLyGiai() {
        try {
            double a = Double.parseDouble(txtA.getText());
            double b = Double.parseDouble(txtB.getText());
            double kq = 0;

            if (radCong.isSelected()) {
                kq = a + b;
            } else if (radTru.isSelected()) {
                kq = a - b;
            } else if (radNhan.isSelected()) {
                kq = a * b;
            } else if (radChia.isSelected()) {
                if (b == 0) {
                    JOptionPane.showMessageDialog(this, "Không thể chia cho 0!");
                    return;
                }
                kq = a / b;
            }

            txtKetQua.setText(String.valueOf(kq));

        } catch (NumberFormatException ex) {
            JOptionPane.showMessageDialog(this,
                    "Vui lòng nhập số hợp lệ cho a và b!");
        }
    }

    private void xuLyXoa() {
        txtA.setText("");
        txtB.setText("");
        txtKetQua.setText("");
        radCong.setSelected(true);
        txtA.requestFocus();
    }

    private void xuLyThoat() {
        int confirm = JOptionPane.showConfirmDialog(
                this,
                "Bạn có chắc muốn thoát?",
                "Xác nhận",
                JOptionPane.YES_NO_OPTION);

        if (confirm == JOptionPane.YES_OPTION) {
            System.exit(0);
        }
    }
}
